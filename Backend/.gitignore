# =================================================================
# BACKEND (NODE.JS)
# =================================================================

# Thư viện phụ thuộc (Không bao giờ push node_modules)
node_modules/

# File chứa biến môi trường và cấu hình nhạy cảm (CỰC KỲ QUAN TRỌNG)
.env
.env.test
.env.production
.env.local
keys/
secrets/

# Log files (Do Node.js hoặc PM2 sinh ra)
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
*.log

# Thư mục build (Nếu team dùng TypeScript sau này)
dist/
build/
out/

# Thư mục chứa file upload tạm thời (nếu có tính năng upload avatar/CV)
uploads/
tmp/
temp/

# Báo cáo test (Coverage)
coverage/
.nyc_output/